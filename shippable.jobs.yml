jobs:
  - name: script-1
    type: runSh
    steps:
      - IN: trigger-1
      - TASK:
        - script: ls
        - script: echo "ran ls 1234"
        - script: xyz
    on_success:
      - NOTIFY: notification-slack
    on_failure:
      - NOTIFY: email
    always:
      - script: echo "always"
      - NOTIFY: notification-slack
      - script: echo "lol"
      - NOTIFY: email
  - name: dv-man-versionName
    type: manifest
    steps:
      - IN: script-1
      - IN: dv-image
        pull: false
        versionName: latest
    always:
      - NOTIFY: notification-slack
  - name: script-2
    type: runSh
    steps:
      - IN: dv-man-versionName
      - IN: trigger-1
      - IN: dv-image
      - TASK:
        - script: pwd
        - script: echo "ran ls 1234"
